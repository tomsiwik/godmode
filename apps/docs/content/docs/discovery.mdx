---
title: Discovery
description: Nested --help walks you through every level of an extension.
---

> TODO: ladder from `godmode --help` → `godmode <ext> --help` → `godmode <ext> <iface> --help` → `godmode <ext> <iface> <resource> --help`. Show a real transcript for stripe. Mention that `--help` is parsed at every level regardless of position; `-h` too. Note that `godmode <ext> --version` shows spec versions.
