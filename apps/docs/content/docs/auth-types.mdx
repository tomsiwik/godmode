---
title: Auth types
description: bearer, api-key, basic — and the header each lands in.
---

> TODO: pull the canonical table from `developers/reference/auth-types` (single source of truth — link there). Summarize in 3 lines: `bearer` → `Authorization: Bearer`, `api-key` → `auth.header`, `basic` → `Authorization: Basic <base64>`. Show the `--> missing <type>` note for each.
