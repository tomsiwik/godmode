---
title: Environment variables
description: Where godmode reads credentials from.
---

> TODO: godmode auto-loads a `.env` from the current working directory at startup (no walking up); shell-exported env takes precedence. Worked example with `STRIPE_API_KEY`. Note no variable expansion, no `.env.local` layering.
