---
title: agent
description: Orchestrate coding-agent workflows through godmode.
---

> TODO: the built-in `agent` extension. Mention harness options (claude, codex, gemini, ...), lifecycle verbs (`start`/`send`/`attach`/`output`/`status`/`list`), and the shortcut `godmode agent <prompt>`. Point at `godmode agent --help`.
