---
title: Local development
description: Iterate on a manifest without publishing.
---

> TODO: `godmode ext install ./folder` reads from disk; edit manifest → `godmode ext update <slug>` picks it up. Note cache location `~/.godmode/apis/<slug>.json`. Show the workflow for debugging a bad spec.
