---
title: MCP serving
description: Serve any extension as an MCP server, even api/graphql ones.
---

> TODO: `godmode <ext> mcp` with no tool starts an MCP server over stdio, regardless of the extension's primary interface. How routes become tools, how params map, when to declare `mcp` explicitly in the manifest vs relying on derivative serving.
