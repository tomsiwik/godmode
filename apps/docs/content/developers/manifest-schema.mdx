---
title: Manifest schema
description: The canonical shape of manifest.yaml.
---

> TODO: render the JSON Schema at `packages/cli/schemas/manifest.schema.json` as a table. Top-level keys (`name`, `slug`, `description`, `interfaces`, `auth`, `headers`), interface-specific nested configs, examples per interface type.
