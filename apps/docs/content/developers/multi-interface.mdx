---
title: Multi-interface extensions
description: Declare api + mcp (or any combination) in one manifest.
---

> TODO: show a manifest with both `api:` and `mcp:` under `interfaces:`. The same extension can be invoked as `godmode <ext> api ...` or `godmode <ext> mcp ...`; `--help` lists both. Use cases and gotchas (auth shared across interfaces, different base URLs per interface).
