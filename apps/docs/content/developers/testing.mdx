---
title: Testing
description: The compliance pipeline and parametrized test matrix.
---

> TODO: `pnpm -r test` runs everything. The 200+ CLI tests cover rule × help-target combinations. How to add a new rule (edit `help-rules.ts`, it auto-applies to every target). The GNU help compliance pipeline (`help2man → mandoc -Tlint → groff -ww`) and how to run it locally.
