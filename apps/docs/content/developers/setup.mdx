---
title: Repo setup
description: Clone, install, build, link.
---

> TODO: `git clone`, `pnpm install`, `pnpm -C packages/cli build`, `pnpm -C packages/cli link --global` for a linked dev binary. Monorepo layout (packages, commands, interfaces, extensions).
