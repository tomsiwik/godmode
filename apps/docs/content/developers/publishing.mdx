---
title: Publishing
description: Ship an extension as @godmode-cli/<slug> on npm.
---

> TODO: package layout (`package.json` with `name`, `exports: { "./manifest": "./manifest.yaml" }`, `files: ["manifest.yaml", "README.md", "LICENSE"]`). `npm publish`. Then anyone can `godmode ext install <slug>`.
